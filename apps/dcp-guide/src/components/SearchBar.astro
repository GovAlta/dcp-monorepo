---
import Search from './Search';
import searchIndexes from '/public/search.json'

const rawIndexData = await Astro.glob('../pages/**/*.{md,mdx}');
const indexes = rawIndexData.filter(data => data.frontmatter.slug);

---
<Search client:load searchList={indexes}></Search>