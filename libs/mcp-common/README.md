# mcp-common

Shared library for MCP (Model Context Protocol) server infrastructure in the DCP monorepo.

Provides transport abstraction (stdio/HTTP), rate limiting, error handling, and server factory utilities.
